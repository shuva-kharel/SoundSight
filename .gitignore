# Secrets -- NEVER commit your real API keys
.env

# Local HTTPS cert (regenerated automatically)
cert.pem
key.pem

# Python
.venv/
__pycache__/
*.pyc

# Banknote classifier: your photos, training runs, and exported models
dataset/
runs/
banknote_runs/

# Voice / faces: local-only data and downloaded models (NEVER commit faces!)
faces_db/
models_vosk/
vosk-model*/
